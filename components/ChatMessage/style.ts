import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        padding: 10,
    },
    messageBox: {
        borderRadius: 10,
        padding: 10,

    },
    content: {
        color: 'white',
        fontSize: 16,

    },
    time: {
        color: 'grey',
        alignSelf: 'flex-end'
    }
})

export default styles;